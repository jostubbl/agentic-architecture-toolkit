# Changelog

## 0.0.2

- Added `Agentic: Init Spec Kit` command that scaffolds `.github/prompts/`, `.github/copilot-instructions.md`, `project-styles.md`, and `manifest.md` in the workspace (existing files are never overwritten)

## 0.0.1

- Initial extension scaffold
- Added `@agentic` chat participant with pipeline slash commands
- Added context menu command bridge for Gauntlet, Orchestrate, War Room, and Code Review
- Added prompt loading with workspace override + bundled defaults
